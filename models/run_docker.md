# Run on docker prior to doing experiments on engr.compute server
"""bash
python3 -m pip install --upgrade pip 
pip3 install matplotlib numpy pandas tqdm category_encoders -U scikit-learn threadpoolctl==3.1.0
"""